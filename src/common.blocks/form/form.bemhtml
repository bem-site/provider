block('form')(
    js()(function () {
        return { message: this.ctx.message };
    }),
    tag()('form'),
    attrs()(function () {
        return { method: 'POST', action: this.ctx.action };
    })
);
